# claude-progress.txt
# 새 컨텍스트 윈도우 시작 시 에이전트가 가장 먼저 읽는 파일
# 세션 종료 시 update-progress.sh가 자동 업데이트
# 수동 업데이트도 가능: 세션 중 중요한 결정이나 발견 사항 기록

---

## 📅 마지막 업데이트
2024-01-01 09:00 (세션 종료)

## 🎯 현재 목표
[현재 스프린트/기능의 최종 목표 한 줄]

## ✅ 마지막으로 완료한 작업
- TASK-001: Contract 엔티티에 ManagerEmail VO 추가 — ✅ 완료 (Critic APPROVE)
- Migration V20240101__add_manager_email.sql 생성 및 적용 — ✅

## 🔄 현재 진행 중인 태스크
- TASK-002: UpdateContractManagerUseCase 구현
  - 중단 지점: ContractRepository에 findByManagerEmail 메서드 추가까지 완료
  - 다음 작업: ApplicationService 구현 시작

## ⚠️ 주의사항 (이번 세션에서 발견)
- Contract 엔티티의 changeManager() 메서드가 이미 존재함 (수정 불필요)
- Oracle 방언 이슈: LIMIT 대신 ROWNUM 사용해야 함
- Checkstyle: 줄 길이 120자 제한 (기존 설정 확인 필요)

## 🐛 발견된 버그 (해결 전)
- ContractQueryRepository.findByStatus() — N+1 문제 있음 (fetch join 미적용)
  → TASK-010으로 별도 트래킹

## 📊 테스트 현재 상태
마지막 확인: ./gradlew test → 142개 통과, 0개 실패
통합 테스트: ./gradlew integrationTest → 23개 통과

## 🗺️ 다음 세션 시작 가이드
1. cat claude-progress.txt (지금 읽고 있는 이 파일)
2. git log --oneline -5 (최근 커밋 확인)
3. cat task-list.md (TASK-002 상세 확인)
4. cat src/application/contract/UpdateContractManagerUseCase.java (중단 지점)
5. ./gradlew compileJava (컴파일 상태 확인)

## 📝 에이전트 메모
- CLAUDE.md 섹션 11에 새 실패 패턴 추가 필요:
  "Oracle에서 페이징 시 ROWNUM 서브쿼리 패턴 사용"
- guard.sh에 추가할 규칙: "ALTER TABLE contract_ 직접 실행 차단"
